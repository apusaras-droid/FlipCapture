D:\AI\00.AI向け開発手順書に過去に作成したプログラムのメモや参考資料あり
CLIをベースに作成し、GUIをラッパーとして使う構造
個人利用専用とし、配布は行わない
ログを残せるようにし、エラーログを回収できるようにする、ユーザーの選択もJSON形式の操作イベントとして記録
プログラム起動用のBATファイルを用意する。
ログインは自動化せず、認証情報はアプリに保存しない
プログラムの設定変更を次回プログラム起動時も引き継げるようにしたい、初期化ボタンもほしい、プリセットとして登録・上書き・削除もしたい
ファイルの保存を求めるプログラムの場合、保存フォルダを開くボタンを追加
プログラムのフォントサイズは11ポイント、フォント種類はZen Kaku Gothic New


簡易パラパラ漫画キャプチャツール 仕様書
バージョン 1.0

1. 概要

本ツールは、マウス操作を使って画面を素早くキャプチャし、保存した画像から簡易的なパラパラ漫画を作成するWindows向けアプリケーションである。

高画質な動画編集や本格的なアニメーション制作は目的とせず、画面上の変化を気軽に連続撮影し、アニメーションWebP、GIF、MP4として出力できることを目的とする。

2. 想定用途

ゲーム画面の簡易パラパラ漫画作成

アプリ操作の短い説明画像作成

CADモデルや図面の変化確認

画像差分の連続表示

キャラクターや図形の簡易アニメーション作成

SNS投稿用の短いループ動画作成

3. 対応環境

対象OS
Windows 10
Windows 11

想定実装言語
Python

主な使用候補ライブラリ
pynput
keyboard
mouse
mss
Pillow
imageio
OpenCV
PySide6またはTkinter

動画変換
FFmpeg

4. 基本操作

4.1 グローバルホットキー

Altキーとマウス操作を組み合わせてキャプチャを実行する。

入力方式は設定画面で次の2種類から選択できる。

ホイール方式（初期値）

Alt＋マウスホイール上
画面全体をキャプチャする。

Alt＋マウスホイール下
設定画面で選択したプログラムのウィンドウ範囲をキャプチャする。

クリック方式

Alt＋マウス左クリック
画面全体をキャプチャする。

Alt＋マウス右クリック
設定画面で選択したプログラムのウィンドウ範囲をキャプチャする。

ホットキーは、アプリが最小化されている状態でも動作する。

4.2 マウス操作の扱い

Alt＋マウスホイール操作を検出した場合、通常のホイール操作は対象アプリへ送信しない。

Alt＋マウスクリック操作を検出した場合、通常の左クリックまたは右クリックは対象アプリへ送信しない。

これにより、キャプチャ時のスクロール、ボタン操作、コンテキストメニュー表示を防ぐ。

Altキー単体の入力が対象アプリのメニューバーへ影響しないよう、必要に応じて入力イベントを抑制する。

5. メイン画面

メイン画面には次の設定項目を表示する。

5.1 キャプチャ対象

画面全体

指定したプログラム

指定プログラムの選択欄には、現在起動中のウィンドウ一覧を表示する。

表示項目

プログラム名

ウィンドウタイトル

プロセス名

ウィンドウハンドル

ユーザーが対象を選択し、設定として保存できる。

5.1.1 キャプチャ入力方式

次の入力方式を選択できる。

Alt＋マウスホイール上下

Alt＋マウス左右クリック

初期値はAlt＋マウスホイール上下とする。

5.2 保存先

キャプチャ画像の保存フォルダを指定する。

保存先はフォルダ選択ダイアログから設定できる。

初期値は次の場所とする。

```text
ユーザーのピクチャフォルダ\FlipCapture
```

5.3 画像形式

初期版ではWebPのみ対応する。

保存形式

WebP

設定項目

画質 1から100

可逆圧縮の有効・無効

初期値

画質 80

可逆圧縮 無効

5.4 ファイル名

キャプチャ画像は連番で保存する。

ファイル名例

```text
capture_000001.webp
capture_000002.webp
capture_000003.webp
```

必要に応じて日時も付加できる。

日時付きの例

```text
capture_000001_20260713_083015.webp
```

5.5 キャプチャ状態

画面上に次の状態を表示する。

ホットキー監視中

監視停止中

選択中のプログラム

保存先

保存枚数

最後に保存した画像名

6. キャプチャ機能

6.1 画面全体キャプチャ

選択中の入力方式に応じて、Alt＋マウスホイール上またはAlt＋マウス左クリックで実行する。

マルチモニター環境では、マウスカーソルがあるモニターのみをキャプチャする。

将来的には次の選択肢を追加可能とする。

全モニター

メインモニター

マウスカーソルがあるモニター

指定モニター

6.2 指定プログラムキャプチャ

選択中の入力方式に応じて、Alt＋マウスホイール下またはAlt＋マウス右クリックで実行する。

選択されたウィンドウの位置とサイズを取得し、その範囲だけをキャプチャする。

可能な場合は、ウィンドウ枠を含めるか除外するかを設定できるようにする。

初期版ではウィンドウ枠を含める。

6.3 キャプチャ失敗時

指定プログラムが終了している場合は、画像を保存しない。

指定プログラムが選択されていない場合は、画像を保存せず、エラーダイアログも表示しない。スキップしたことをログにのみ記録する。

通知例

```text
指定したウィンドウが見つかりません。
```

ウィンドウが最小化されている場合は、キャプチャできないことを通知する。

保存先へ書き込めない場合は、エラー内容を表示する。

6.4 キャプチャ通知

キャプチャ成功時は、小さな通知または効果音を出せるようにする。

設定項目

通知表示 有効・無効

効果音 有効・無効

初期値

通知表示 有効

効果音 無効

通知例

```text
capture_000012.webp を保存しました。
```

7. キャプチャ画像一覧

メイン画面からキャプチャ画像一覧画面を開ける。

表示内容

サムネイル

ファイル名

撮影日時

画像サイズ

ファイル容量

操作

画像を選択

複数選択

削除

保存フォルダを開く

別のフォルダから画像を追加

選択画像を範囲指定してトリミング

トリミング結果で元画像を上書き

トリミング結果を元のファイル名に「_trim」を付けた別画像として保存

選択範囲内の人物、キャラクター、小物をAIで抽出し、背景を透過したWebPとして保存

画像中央に近い主要な人物、キャラクター、小物をAIで自動検出し、トリミング範囲を設定

フリーハンドで対象を囲み、範囲外を透明化したうえで範囲内の対象輪郭をAIで抽出

画像をフルカラーSVG、輪郭線SVG、シルエットSVGおよびDXFへベクター変換

DXFは外形線のみ、または外形線と内部線の両方を選択して出力

すべて選択

選択解除

8. パラパラ漫画作成画面

8.1 画像選択

保存済みのWebP画像から、アニメーションに使用する画像を選択する。

複数選択に対応する。

選択した画像は、ファイル名順または撮影日時順で並べる。

初期状態ではファイル名順とする。

8.2 画像の並べ替え

次の操作を提供する。

上へ移動

下へ移動

先頭へ移動

最後へ移動

削除

逆順

ファイル名順

撮影日時順

ドラッグ操作による並べ替えは将来対応とし、初期版ではボタン操作だけでもよい。

8.3 表示時間

すべての画像に共通する表示時間を設定する。

単位

ミリ秒

初期値

150ミリ秒

設定範囲

30ミリ秒から5000ミリ秒

プリセット

高速 80ミリ秒

標準 150ミリ秒

ゆっくり 300ミリ秒

8.4 ループ設定

無限ループ

1回のみ

指定回数

初期値

無限ループ

8.5 サイズ設定

元画像のサイズを維持

横幅を指定

縦幅を指定

比率を指定

初期版では次の2種類に対応する。

元画像のサイズを維持

横幅を指定して縦横比を維持

横幅の初期値

1280ピクセル

8.6 プレビュー

出力前に簡易プレビューを表示する。

操作

再生

停止

先頭へ戻る

前の画像

次の画像

再生速度変更

プレビューは画質確認ではなく、画像の順番と速度確認を目的とする。

9. 出力形式

9.1 アニメーションWebP

標準出力形式とする。

特徴

フルカラー

比較的軽量

ループ再生可能

Webブラウザーで確認可能

設定項目

画質

表示時間

ループ回数

画像サイズ

初期画質

80

9.2 GIF

互換性重視の出力形式とする。

特徴

多くのサービスで表示可能

短いループアニメーション向け

色数は最大256色

設定項目

表示時間

ループ回数

画像サイズ

初期版では細かな減色設定は行わず、自動変換とする。

9.3 MP4

SNS投稿向けの動画形式とする。

映像形式

H.264

音声

なし

ピクセル形式

yuv420p

フレームレート

30fps

画像の表示時間に合わせて、同じ画像を複数フレーム配置して出力する。

動画の縦横サイズが奇数の場合は、エンコード可能な偶数サイズへ自動調整する。

9.4 出力先

初期値はキャプチャ画像と同じフォルダとする。

出力ファイル名例

```text
animation_20260713_083500.webp
animation_20260713_083500.gif
animation_20260713_083500.mp4
```

10. 基本的な利用手順

11. アプリを起動する。

12. キャプチャ画像の保存先を選択する。

13. 必要に応じてキャプチャ対象プログラムを選択する。

14. 選択した入力方式の全画面キャプチャ操作を実行する。

15. 選択した入力方式の指定プログラムキャプチャ操作を実行する。

16. 必要な枚数だけ繰り返し撮影する。

17. パラパラ漫画作成画面を開く。

18. 使用する画像を選択する。

19. 順番と表示時間を設定する。

20. プレビューを確認する。

21. アニメーションWebP、GIF、MP4のいずれかで出力する。

22. 設定保存

アプリ終了時に設定を保存する。

保存対象

画像保存先

キャプチャ入力方式

選択したプログラム

WebP画質

可逆圧縮設定

通知設定

効果音設定

表示時間

ループ設定

出力サイズ

最後に使用した出力形式

設定ファイル形式

JSON

保存例

```json
{
  "capture_directory": "C:\\Users\\User\\Pictures\\FlipCapture",
  "capture_input_mode": "wheel",
  "webp_quality": 80,
  "webp_lossless": false,
  "notification_enabled": true,
  "sound_enabled": false,
  "default_frame_duration_ms": 150,
  "default_loop_count": 0,
  "default_output_format": "webp",
  "target_window_title": "",
  "target_process_name": ""
}
```

12. 画面構成

12.1 メイン画面

キャプチャ監視開始ボタン

起動時のキャプチャ監視自動開始設定

キャプチャ監視停止ボタン

キャプチャ入力方式選択

起動時に管理者権限を要求する設定

保存先設定

キャプチャ対象プログラム選択

第2キャプチャ対象を指定プログラムまたはホットキー操作ごとに選択する矩形範囲から選択

Alt＋マウスホイールクリックで矩形範囲を選択し、OCR結果をクリップボードへコピー

画像画質設定

通知設定

現在の状態表示

保存フォルダを開くボタン

画像一覧を開くボタン

パラパラ漫画作成ボタン

12.2 画像一覧画面

画像サムネイル一覧

複数選択

削除

追加

保存フォルダを開く

パラパラ漫画作成画面へ送る

12.3 パラパラ漫画作成画面

選択画像一覧

順番変更ボタン

削除ボタン

逆順ボタン

表示時間設定

ループ設定

画像サイズ設定

出力形式選択

プレビュー

出力ボタン

13. エラー処理

保存先が存在しない場合は、自動作成を試みる。

保存先を作成できない場合は、別の保存先を選択させる。

画像が1枚しか選択されていない場合は、アニメーションを作成しない。

対応していない画像形式が選択された場合は、その画像を除外して通知する。

FFmpegが見つからない場合は、MP4出力を無効化する。

GIFまたはWebP出力に失敗した場合は、原因をログへ記録する。

14. ログ

動作確認用に簡易ログを保存する。

ログ内容

アプリ起動

監視開始

監視停止

キャプチャ成功

キャプチャ失敗

画像出力成功

画像出力失敗

FFmpeg実行結果

ログ保存先

```text
アプリフォルダ\logs
```

15. 初期版で実装しない機能

画像への文字入力

吹き出し追加

画像への手書き

音声追加

動画編集

フレームごとの個別表示時間

回転や傾き補正を伴う高度な画像切り抜き編集

フィルター加工

クラウド保存

SNSへの直接投稿

高度なGIF減色設定

複数レイヤー編集

16. 将来追加できる機能

キャプチャ操作への任意キー割り当て

マウスサイドボタンへの割り当て

特定範囲を矩形選択してキャプチャ

一定時間ごとの自動キャプチャ

連続キャプチャモード

フレームごとの表示時間設定

ドラッグによる画像並べ替え

文字や吹き出しの追加

画像比較モード

透過WebP対応

APNG出力

WebM出力

クリップボードへのコピー

撮影直後の取り消し

キャプチャ時のマウスカーソル表示切り替え

17. 初期版の実装優先順位

優先度1

Alt＋ホイールまたはAlt＋左右クリックによるグローバル入力検出

画面全体キャプチャ

指定ウィンドウキャプチャ

WebP保存

保存先設定

画質設定

優先度2

画像一覧

複数画像選択

並べ替え

アニメーションWebP出力

GIF出力

優先度3

MP4出力

プレビュー

通知機能

設定保存

ログ保存

18. 完成条件

次の条件を満たした時点で初期版完成とする。

設定でホイール方式と左右クリック方式を切り替えられる。

選択した入力方式で画面全体を保存できる。

選択した入力方式で指定ウィンドウを保存できる。

画像をWebP形式で連番保存できる。

保存先と画質を変更できる。

保存した画像を複数選択できる。

画像の順番を変更できる。

表示時間を設定できる。

アニメーションWebPを作成できる。

GIFを作成できる。

FFmpegが利用可能な環境ではMP4を作成できる。

アプリを再起動しても設定が保持される。

19. 開発方針

本ツールは、本格的なアニメーション編集ソフトではなく、気軽に撮影して遊べることを重視する。

初期版では操作数を減らし、次の流れを最短で実行できることを優先する。

```text
選択したマウス操作で撮影
↓
画像を選択
↓
速度を決定
↓
形式を選択
↓
出力
```

細かな編集機能よりも、起動の速さ、キャプチャのしやすさ、出力までの手軽さを優先する。
