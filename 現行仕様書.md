# FlipCapture 現行仕様書

バージョン: 1.0  
基準日: 2026年7月13日  
対象OS: Windows 10 / Windows 11

## 1. 文書の目的

本書は、現在実装されているFlipCaptureの動作を基準にした仕様書である。将来構想ではなく、現行プログラムが提供する画面キャプチャ、画像編集、AI背景透過、アニメーション作成、ベクター変換およびCLI機能を記載する。

## 2. 製品概要

FlipCaptureは、グローバルなマウス操作で画面をWebP画像として保存し、保存画像から簡易アニメーションや透過切り抜き画像、SVG、DXFを作成する個人利用向けWindowsアプリケーションである。

構造はCLIを中核とし、Tkinter GUIを操作用ラッパーとして提供する。認証情報の保存やログイン操作の自動化は行わない。

## 3. システム構成

### 3.1 実装言語と主要技術

- Python 3
- Tkinter: GUI
- Windows API: ウィンドウ列挙、低レベルマウスフック、管理者権限判定
- Windows Runtime OCR: 日本語文字認識（子プロセス隔離）
- mss: 画面キャプチャ
- Pillow: WebP保存、画像編集、アニメーションWebP/GIF
- Zen Kaku Gothic New: GUI表示フォント（SIL Open Font License 1.1、アプリ内同梱）
- FFmpeg: H.264 MP4出力。起動時検証とローカル配置検出に対応
- rembg / ONNX Runtime: AI背景透過
- SciPy / scikit-image: 自動検出、輪郭追跡
- VTracer: フルカラーSVG変換
- ezdxf: DXF生成

### 3.2 ディレクトリ構成

```text
スクショツール\
├─ flipcapture\          プログラム本体
├─ config\
│  └─ settings.json      ユーザー設定
├─ locales\              組み込み・ユーザー追加言語のJSON
├─ assets\fonts\         GUI同梱フォントとライセンス
├─ logs\                 動作ログ
├─ tests\                自動テスト
├─ requirements.txt      Python依存関係
├─ setup.bat             仮想環境作成・依存関係導入
├─ start_flipcapture.bat 通常起動
└─ start_debug.bat       コンソール付きデバッグ起動
```

## 4. 起動

### 4.1 初回セットアップ

`setup.bat`を実行し、`.venv`仮想環境を作成して`requirements.txt`の依存関係を導入する。

### 4.2 通常起動

`start_flipcapture.bat`から`pythonw -m flipcapture gui`を起動する。仮想環境が存在する場合は仮想環境内のPythonを優先する。

### 4.3 デバッグ起動

`start_debug.bat`からコンソール付きで起動する。起動エラー時はコンソールを閉じず、内容を確認できる。

### 4.4 管理者権限

「起動時に管理者権限を要求」が有効で、現在のプロセスが通常権限の場合は、Windows標準のUAC確認を経由してGUIを再起動する。認証情報はアプリに保存しない。

### 4.5 GUIフォント

GUIは同梱したZen Kaku Gothic NewのRegularおよびBoldを、FlipCaptureプロセス内だけで一時登録して使用する。Windowsのフォントフォルダへ恒久インストールしない。フォントファイルが存在しない、または登録できない場合はWindows標準UIフォントへフォールバックし、アプリの起動は継続する。ライセンス文は`assets\fonts\OFL-ZenKakuGothicNew.txt`へ同梱する。

### 4.6 表示言語

初期表示言語は英語とし、設定画面から英語または日本語へ切り替えられる。言語変更時は設定を保存し、ホットキー監視とトレイを安全に終了して、新しい言語でGUIを自動再起動する。

言語定義は起動時に`locales\*.json`から読み込む。ユーザーはUTF-8 JSONファイルを追加するだけで任意言語を追加でき、プログラム本体の変更を必要としない。

```json
{
  "code": "fr",
  "name": "Français",
  "translations": {
    "入力方式": "Mode de saisie",
    "監視開始": "Démarrer la surveillance"
  }
}
```

`code`は一意の言語コード、`name`は選択欄へ表示する名称、`translations`は日本語原文をキーとした翻訳辞書とする。翻訳がない文字列は日本語原文へフォールバックする。不正な言語ファイルは無視してログへ記録し、アプリの起動は継続する。追加・編集後はFlipCaptureを再起動する。

### 4.7 タスクトレイ常駐

起動時にWindowsの通知領域へFlipCaptureアイコンを登録する。「閉じるボタンでタスクトレイに格納」が有効な場合、閉じるボタンはプロセスを終了せずGUIだけを隠し、ホットキー監視を継続する。トレイメニューから画面表示、監視開始／停止、保存フォルダ表示、完全終了を実行できる。

「起動時からタスクトレイに格納」を有効にした場合は、起動処理後にGUIを隠す。トレイ初期化に失敗した場合はタスクバーへ最小化し、画面から再操作できる状態を維持する。「閉じるボタンでタスクトレイに格納」を無効にした場合は、閉じるボタンで完全終了する。

## 5. グローバルキャプチャ操作

### 5.1 入力方式

次の2方式から選択する。

| 入力方式 | カーソル位置モニター | 第2キャプチャ |
|---|---|---|
| ホイール方式 | Alt＋ホイール上 | Alt＋ホイール下 |
| クリック方式 | Alt＋左クリック | Alt＋右クリック |

初期値はホイール方式とする。該当操作を検出した場合、そのマウスイベントは操作中のアプリへ送信しない。

入力方式にかかわらず、Alt＋マウスホイールクリック（中ボタン）はOCR範囲選択を起動する。

### 5.2 監視開始

初期設定ではGUI起動から約0.5秒後にホットキー監視を自動開始する。「起動時にホットキー監視を自動開始」を無効にした場合は、「監視開始」ボタンで手動開始する。

### 5.3 カーソル位置モニターのキャプチャ

マウスカーソル座標を取得し、その座標を含むモニター1台だけをキャプチャする。仮想デスクトップ全体は保存しない。負のモニター座標にも対応する。

### 5.4 指定ウィンドウのキャプチャ

起動中で可視状態のトップレベルウィンドウを一覧表示し、次の情報を保持する。

- ウィンドウタイトル
- プロセス名
- プロセスID
- ウィンドウハンドル

選択したウィンドウの外枠を含む矩形をキャプチャする。対象が最小化されている場合、終了済みの場合、範囲取得に失敗した場合は画像を保存しない。

対象ウィンドウが一度も選択されていない場合は、エラーダイアログを表示せず処理をスキップし、ログにのみ記録する。

### 5.5 矩形範囲のキャプチャ

第2キャプチャを「矩形範囲」へ切り替えると、ホットキー操作のたびにFlipCapture自身を一時的に隠し、マウスカーソルがあるモニター1枚を静止画像として取得する。その静止画像を対象モニター内へフィット表示し、ドラッグで矩形を指定する。マウスボタンを離した時点では保存せず、Enterキーで指定範囲を原寸画像から切り抜いてWebP保存する。Escキーで保存せずに中止する。

矩形範囲モードでは使用しない指定ウィンドウのラベル、選択欄、更新ボタンを非表示にする。第2キャプチャを指定ウィンドウへ戻した場合は再表示する。

Tkinterの最初のウィンドウを生成する前にWindowsのPer-Monitor V2 DPI awarenessを有効化する。これによりMSS、Tkinter、マウス座標はいずれも対象モニター固有の物理ピクセル座標を使用する。100%と150%など異なる拡大率のモニターを併用しても、仮想デスクトップ全体の平均倍率は使用せず、カーソル位置モニターのleft、top、width、heightへ選択画面を直接配置する。負の座標および上下左右に配置した複数モニターへ対応する。

Per-Monitor V2によりGUI寸法が物理ピクセル単位となるため、保存済みウィンドウ寸法がない初回起動時は対象DPIに応じてメインウィンドウと最小寸法を拡大する。保存済み寸法がある場合は前回値を優先する。

### 5.6 手動キャプチャ

GUIから次の操作をホットキーなしで実行できる。

- カーソル位置のモニターを撮影
- 指定ウィンドウを撮影
- その都度選択する矩形範囲を撮影

### 5.7 OCR範囲選択

Alt＋マウスホイールクリックを検出すると、FlipCapture自身を一時的に隠し、マウスカーソルがあるモニター1枚を静止画像として取得して画面内へフィット表示する。ドラッグで文字範囲を指定し、EnterでWindows Runtimeの日本語OCRを実行する。認識結果は整形後にWindowsクリップボードへコピーする。Escで中止する。

OCR画質は「標準」「高精度」「小さい文字」から選択し、初期値は「高精度」とする。「標準」は色を保持し、画像が大きすぎる場合だけ縮小する。「高精度」は最大2倍拡大とグレースケール・自動コントラスト・コントラスト強調・シャープ補正、「小さい文字」は最大3倍拡大と強めのコントラスト補正を行う。Windows OCRの画像寸法上限を考慮し、すべてのモードで長辺は4000px以内とする。

OCR処理はPC内で完結し、画像や認識結果を外部サービスへ送信しない。WinRT/COMとTkinterのスレッドモデルを分離するため、OCRは仮想環境の`python.exe`を使った専用子プロセスで実行し、45秒でタイムアウトする。入力画像は一時PNG、認識結果はUTF-8 JSONの一時ファイルで受け渡し、`pythonw.exe`の標準入出力およびWindowsコンソール文字コードには依存しない。一時ファイルは処理終了後に削除する。子プロセス異常終了はGUIプロセスを巻き込まずエラーとして通知する。日本語OCRを利用できない場合は、Windowsの日本語言語パック確認を案内する。

OCR結果をクリップボードへ反映するGUIイベントで例外が発生した場合はスタックトレースをログへ記録し、後続のGUIイベント監視を継続する。

## 6. キャプチャ画像保存

### 6.1 保存形式

- 形式: WebP
- 画質: 1～100、初期値80
- 可逆圧縮: 有効／無効、初期値無効

### 6.2 保存先

初期値は`ユーザーのPictures\FlipCapture`とする。フォルダ選択ダイアログから変更できる。保存先が存在しない場合は自動作成する。

保存先欄の「開く」または画面下部の「保存フォルダを開く」から、現在の保存先をエクスプローラーで開く。

### 6.3 ファイル名

```text
capture_000001.webp
capture_000002.webp
```

「ファイル名に日時を付加」が有効な場合:

```text
capture_000001_20260713_100159.webp
```

保存先を初めて使用する時点で既存`capture_*.webp`から最大連番を1回だけ取得し、以降はメモリ上のカウンターをロック内で増加させる。保存先を変更した場合だけ再走査する。

## 7. メインGUI

GUIは「キャプチャ」と「パラパラ漫画」の2タブで構成する。

### 7.1 キャプチャタブ

- 入力方式選択
- 保存先入力、選択、フォルダを開く
- 指定ウィンドウ選択、一覧更新
- WebP画質
- 可逆圧縮
- 日時付きファイル名
- 通知表示
- 効果音
- 起動時に管理者権限を要求
- 現在の権限表示
- 起動時にホットキー監視を自動開始
- 監視開始／停止
- 手動キャプチャ
- 状態および処理結果表示
- キャプチャ画像を一覧へ追加

### 7.2 パラパラ漫画タブ

- 画像一覧
- 複数画像追加
- 一覧から削除（元ファイルは削除しない）
- 上下移動
- 逆順
- トリミング画面
- 回転画面
- ベクター化画面
- 選択画像プレビュー
- アニメーション出力設定

## 8. 画像一覧とプレビュー

対応入力形式はWebP、PNG、JPEG、BMPとする。保存フォルダから読み込む場合は`capture_*.webp`をファイル名順で追加する。

同一パスは重複登録しない。一覧で選択した画像を最大430×520ピクセル相当へ縮小して表示する。

## 9. トリミング

### 9.1 矩形選択

画像上をドラッグして矩形を指定する。表示倍率と余白を考慮して元画像座標へ変換する。選択範囲は2×2ピクセル以上必要とする。

### 9.2 フリーハンド選択

マウス軌跡で対象を囲む。囲みの外側を透明化し、境界へ軽いぼかしを適用する。フリーハンド使用時は透過WebPとして保存する。

AI背景透過と併用した場合、フリーハンド範囲を制約として使用し、範囲内でAIが対象輪郭を抽出する。

### 9.3 中央対象の自動検出

画像を最大1280ピクセルへ縮小して前景マスクを生成する。前景候補を連結成分へ分離し、面積と画像中央への近さから対象を選択する。弱く検出された字幕やUIを除外するため、マスク強度しきい値を適用する。

検出結果は矩形として表示し、保存前に手動で指定し直せる。自動検出後はAI背景透過を自動的に有効にする。

### 9.4 AI背景透過

次のモデルを選択できる。

| モデル | 用途 |
|---|---|
| u2net | 人物、小物などの汎用抽出 |
| isnet-anime | イラスト、キャラクター抽出 |

モデルは初回利用時に自動取得し、ユーザーディレクトリのrembgモデルキャッシュへ保存する。取得中は進捗率を表示する。接続タイムアウト10秒、データ待機タイムアウト30秒、全体タイムアウト180秒を適用し、キャンセル操作を受け付ける。取得完了後はMD5を検証し、一時ファイルを同期してから置換する。画像は外部サービスへ送信せず、推論はPC内で行う。

`isnet-anime`では字幕や背景色の混入を抑えるため、追加のアルファマッティングを使用しない。

### 9.5 保存方法

- 元画像を上書き
- 別名保存

別名保存の初期名:

```text
元の名前_trim.webp
元の名前_trim_2.webp
```

同名が存在する場合は連番を付ける。AI背景透過またはフリーハンド使用時は常にWebPへ出力する。元画像がWebP以外の場合、透過結果の上書きは許可せず別名保存を案内する。

上書きは一時ファイルへ保存してから置換する。上書き前に確認ダイアログを表示する。

### 9.6 画像回転

画像一覧で画像を1枚選択して「回転」を押すと、回転・傾き補正用の専用プレビュー画面を開く。左90度、右90度、180度、元に戻すを選択でき、複数回の操作結果を保存前に確認できる。90度回転時は縦横寸法を入れ替え、画像全体が欠けないようキャンバスを拡張する。透過画像のアルファチャンネルは維持する。

「自動で傾きを補正」は、解析用画像を長辺1600px以内へ縮小し、Cannyエッジ検出と確率的ハフ変換で長い直線を抽出する。各直線について水平または垂直に近い方からのずれを求め、線分長で重み付けした中央値を補正角度とする。初期状態では±20度以内を対象とし、検出線数と信頼度を画面へ表示する。解析はバックグラウンドスレッドで行い、Tkinterの操作はメインスレッドで継続する。十分な直線を検出できない場合は画像を変更せず、手動指定を案内する。

「基準線を手動指定」は、プレビュー画像内で基準にする直線の始点と終点を順にクリックする。指定線が水平と垂直のどちらに近いかを自動判定し、近い方へ揃える補正角度を適用する。短すぎる基準線や画像外のクリックは採用しない。補正後の画像は保存前にプレビューできる。

保存方法は元画像への上書き、またはWebPでの別名保存から選択する。別名保存の初期名は`元の名前_rotated.webp`とし、同名ファイルが存在する場合は`_rotated_2`以降の連番を付けて既存ファイルを保護する。上書き時は確認ダイアログを表示し、PNG、JPEG、WebPの元形式を維持する。保存は一時ファイル作成後に置換する。

## 10. アニメーション作成

### 10.1 共通設定

- 必要画像数: 2枚以上
- 表示時間: 30～5000ミリ秒、初期値150
- ループ回数: 0を無限ループとして扱う
- 横幅: 0で元サイズ、1以上で縦横比を維持してリサイズ
- 異なる画像サイズは最大幅・最大高の黒いキャンバスへ中央配置

### 10.2 アニメーションWebP

- フルカラー
- 画質指定対応
- ループ対応
- ファイル名例: `animation_YYYYMMDD_HHMMSS.webp`

### 10.3 GIF

- 256色へ自動減色
- ループ対応
- 最適化保存

### 10.4 MP4

- 映像コーデック: H.264 / libx264
- フレームレート: 30fps
- ピクセル形式: yuv420p
- 音声: なし
- Web向けfaststart有効
- 奇数サイズは黒余白を加えて偶数サイズへ調整
- FFmpeg未検出時はGUIのMP4選択肢を無効化し、CLIでは明示的エラー

GUI起動時にFFmpegを次の順で検証する。

1. `tools\ffmpeg\bin\ffmpeg.exe`
2. `tools\ffmpeg.exe`
3. 環境変数PATH

## 11. ベクター化

画像一覧で画像を1枚選択し、「ベクター化」から方式を選択する。

### 11.1 フルカラーSVG

VTracerにより色と陰影を積層パスへ変換する。処理負荷とパス数を抑えるため、長辺が1800ピクセルを超える場合は縮小して変換する。

透過画像は、透明部分のRGBを白へ正規化してからトレースする。元画像のアルファ外形からSVGクリップパスを生成し、背景色が外形と結合しないよう全カラーパスをクリップする。画像が全面不透明な場合はSVGビューポート全体が有効領域であるためクリップを省略し、通常のRGBスクリーンショットでも変換を失敗させない。

出力名:

```text
元の名前_vector_color.svg
```

### 11.2 輪郭線SVG

グレースケール画像へCannyエッジ検出を適用し、主要な内部境界をSVGの開いたパスとして出力する。透明部分との外形境界も追加する。

出力名:

```text
元の名前_vector_outline.svg
```

### 11.3 シルエットSVG＋DXF

アルファチャンネルの境界からシルエットを生成する。画像配列の外側は透明として1ピクセル分パディングしてから輪郭を追跡するため、全面不透明なスクリーンショットでは画像外周を閉じたシルエットとしてSVGとDXFへ出力する。前景が存在せず輪郭が空の場合は成功扱いにせずエラーを返す。

SVGは黒塗りパスをeven-odd規則で出力する。DXFはR2010形式とし、1画像ピクセルを1作図単位として扱う。

DXF出力方式:

- 外形線のみ
- 外形線＋内部線

| DXFレイヤー | 内容 | 図形 |
|---|---|---|
| SILHOUETTE | 外形および透明な穴 | 閉じたLWPOLYLINE |
| INTERNAL | 目、口、髪、服などの内部境界 | 開いたLWPOLYLINE |

出力名:

```text
元の名前_vector_silhouette.svg
元の名前_vector_silhouette.dxf
元の名前_vector_silhouette_internal.dxf
```

## 12. 設定保存

設定は`config/settings.json`へUTF-8 JSONで保存する。既存設定を`settings.json.bak`へバックアップし、一時ファイルをflushおよびfsyncした後に`os.replace`で置換する。置換失敗時に本体が失われている場合はバックアップから復元する。読み込み時に本体が破損している場合もバックアップを使用する。

設定項目の変更時、ウィンドウをタスクトレイへ格納する時、および完全終了時に現在値を保存する。次回起動時は保存された入力方式、保存先、OCR画質、キャプチャ方式、管理者・通知・トレイ設定、アニメーション設定、前回のタブ、ウィンドウ位置とサイズを復元する。設定読み込み中にGUIイベントが発生しても保存処理を行わず、読み込み途中の初期値で既存設定を上書きしない。

### 12.1 設定項目

| キー | 初期値 | 内容 |
|---|---:|---|
| language | en | 表示言語コード。初期値は英語 |
| capture_directory | Pictures\\FlipCapture | キャプチャ保存先 |
| capture_input_mode | wheel | wheel / click |
| run_as_admin | false | GUI起動時に管理者権限を要求 |
| monitoring_autostart | true | 起動時に監視開始 |
| close_to_tray | true | 閉じるボタンでタスクトレイへ格納 |
| start_minimized_to_tray | false | 起動時からタスクトレイへ格納 |
| window_geometry | 空文字 | 前回のウィンドウ位置とサイズ |
| selected_tab | capture | capture / animation |
| secondary_capture_mode | window | window / region |
| ocr_quality_mode | high | standard / high / small_text |
| webp_quality | 80 | WebP画質 |
| webp_lossless | false | 可逆圧縮 |
| notification_enabled | true | エラー等の画面通知 |
| sound_enabled | false | キャプチャ成功音 |
| include_timestamp | false | ファイル名へ日時付加 |
| default_frame_duration_ms | 150 | アニメーション表示時間 |
| default_loop_count | 0 | ループ回数 |
| default_output_format | webp | 前回の出力形式 |
| output_width | 0 | アニメーション出力幅 |
| target_window_handle | 0 | 対象ウィンドウハンドル |
| target_window_title | 空文字 | 対象タイトル |
| target_process_name | 空文字 | 対象プロセス名 |

設定ファイルが存在しない、JSONが不正、型変換に失敗した場合は初期設定を使用する。未知のキーは無視する。

## 13. ログ

ログは次の場所へUTF-8で保存する。

```text
logs\flipcapture_YYYYMMDD.log
```

標準ログ形式:

```text
日時 ログレベル ロガー名: メッセージ
```

ユーザー操作と処理結果は、`EVENT`に続くJSONとして同じログへ記録する。

主なイベント:

- app_started / app_stopped
- settings_changed
- language_changed
- monitoring_started / monitoring_stopped
- window_hidden_to_tray / window_restored_from_tray
- target_window_selected
- capture_success / capture_failure
- ocr_region_selected / ocr_worker_started / ocr_worker_finished
- ocr_worker_timeout / ocr_worker_failure / ocr_result_received
- ocr_success / ocr_failure
- image_trimmed
- image_rotated
- deskew_auto_detected / deskew_manual_selected / deskew_auto_saved
- subject_auto_detected
- animation_success / animation_failure
- vectorize_success / vectorize_failure
- admin_restart_requested

未捕捉例外は`sys.excepthook`でスタックトレースを記録する。FFmpegの終了コードと標準エラーもログへ記録する。

## 14. エラー処理

- 保存先がない場合は自動作成する。
- 保存不能、画面取得不能、ウィンドウ終了、最小化はエラーとして記録する。
- 対象ウィンドウ未選択は画面通知せずログのみとする。
- アニメーション画像が2枚未満の場合は処理しない。
- 非対応画像はアニメーション入力から除外する。
- FFmpeg未検出時はGUIのMP4を無効化し、CLIでは明示的エラーを返す。
- AIモデル取得は進捗表示、キャンセル、通信および全体タイムアウト、MD5検証を行う。
- 設定置換失敗時はバックアップから復元する。
- OCRのWinRT処理はタイムアウト付き子プロセスへ隔離し、結果はUTF-8 JSONで受け渡す。
- OCR結果のGUI反映に失敗してもイベント監視を停止せず、例外をログへ記録する。
- 自動傾き補正で十分な直線を検出できない場合は保存せず、手動基準線を案内する。
- 不正な言語ファイルはそのファイルだけを無視し、アプリの起動を継続する。
- タスクトレイを初期化できない場合はタスクバーへ最小化し、終了不能な状態を避ける。
- AI依存関係未導入時は`setup.bat`の再実行を案内する。
- AI処理とベクター処理はバックグラウンドスレッドで実行し、GUIの長時間停止を避ける。
- AI処理中は保存ボタンと閉じる操作を抑止する。

## 15. CLI仕様

### 15.1 GUI

```powershell
python -m flipcapture gui
```

コマンド省略時もGUIを起動する。

### 15.2 ウィンドウ一覧

```powershell
python -m flipcapture windows
```

1ウィンドウにつき1行のJSONを標準出力へ表示する。

### 15.3 キャプチャ

```powershell
python -m flipcapture capture-full --output-dir C:\captures
python -m flipcapture capture-window --hwnd 123456 --output-dir C:\captures
python -m flipcapture capture-region --left 100 --top 100 --width 1280 --height 720
```

`capture-full`はカーソル位置モニターを意味する。

### 15.4 アニメーション

```powershell
python -m flipcapture animate frame1.webp frame2.webp \
  -o animation.webp --duration 150 --loop 0 --width 1280 --quality 80
```

出力拡張子でWebP、GIF、MP4を判定する。

### 15.5 ベクター化

```powershell
python -m flipcapture vectorize image.webp --mode color
python -m flipcapture vectorize image.webp --mode outline
python -m flipcapture vectorize image.webp --mode silhouette
python -m flipcapture vectorize image.webp --mode silhouette --dxf-internal
```

### 15.6 設定表示

```powershell
python -m flipcapture settings
```

現在の設定をJSONで標準出力へ表示する。

### 15.7 OCR

```powershell
python -m flipcapture ocr image.png --quality high
```

Windows標準OCRで認識した文字列を標準出力へ表示する。

`--quality`は`standard`、`high`、`small_text`から指定し、省略時は`high`とする。

### 15.8 画像回転

```powershell
python -m flipcapture rotate image.webp --angle 90
python -m flipcapture rotate image.webp --angle -90 --overwrite
python -m flipcapture rotate image.png --angle 180 -o rotated.webp --quality 90
python -m flipcapture rotate image.png --angle -2.5 -o corrected.webp
python -m flipcapture deskew image.png -o corrected.webp --max-angle 20
```

`rotate`の正数は左回転、負数は右回転とし、小数角度にも対応する。`deskew`は長い直線から補正角度を自動検出し、補正角度、検出線数、信頼度を標準エラーへ表示する。`--max-angle`の初期値は20度とする。`--overwrite`と`--output`は同時指定できない。

## 16. 性能および運用上の注意

- 高解像度画像のAI推論はCPU負荷とメモリを使用する。
- OCRの「高精度」「小さい文字」は拡大と画像補正を行うため、「標準」より処理時間とメモリ使用量が増える。写真上の色付き文字では「標準」の方が良好な場合がある。
- AIモデルは各約数百MB規模で、初回のみ取得時間がかかる。
- フルカラーSVGと内部線付きDXFはパス数が多くなり、元画像によってはファイルが大きくなる。
- 内部線付きDXFはCAD上での編集性より画像再現性を優先する。加工データとして使う場合は線の整理が必要になることがある。
- 管理者権限で動作するアプリ上のグローバル入力を取得するには、FlipCapture側も同等の権限が必要になる場合がある。
- ウィンドウハンドルはアプリ再起動後に変わる可能性があるため、対象が見つからない場合は一覧を更新して再選択する。

## 17. 現行版で未実装の機能

- キャプチャ時のマウスカーソル合成
- 任意キーへの完全なホットキー割り当て
- 指定ウィンドウの枠を除外する設定
- フレームごとの個別表示時間
- アニメーションの連続再生プレビュー
- ドラッグ＆ドロップによるフレーム並べ替え
- 画像への文字、吹き出し、手書き追加
- 音声付き動画
- SNSおよびクラウドへの直接投稿
- DXF内部線の簡略化強度設定

## 18. 完成確認項目

- 起動時にホットキー監視が自動開始される。
- 初期状態で英語表示され、日本語へ切り替えて自動再起動できる。
- `locales`へ追加した有効な言語JSONが選択肢へ表示される。
- 閉じるボタンでタスクトレイへ格納され、ホットキー監視が継続する。
- トレイメニューから画面表示、監視開始停止、保存フォルダ表示、完全終了ができる。
- Alt＋マウスホイールクリックで範囲OCRを起動し、結果をクリップボードへコピーできる。
- OCRと矩形選択はカーソル位置のモニター1枚だけを表示し、DPI拡大時も画面内へ収まる。
- OCR画質を標準、高精度、小さい文字から選択でき、設定が再起動後も保持される。
- OCR子プロセスの異常終了やタイムアウトでGUI本体が終了しない。
- 2種類の入力方式を切り替えられる。
- カーソル位置モニターをWebP保存できる。
- 指定ウィンドウをWebP保存できる。
- 未選択ウィンドウ操作を通知なしでスキップできる。
- 設定が再起動後も保持される。
- アニメーション設定、前回のタブ、ウィンドウ位置とサイズが次回起動時に復元される。
- 矩形、フリーハンド、自動検出でトリミング範囲を作成できる。
- 画像を左90度、右90度、180度へ回転し、上書きまたは別名で保存できる。
- 長い直線を使った自動傾き補正、および2点の手動基準線による傾き補正ができる。
- AIで汎用対象およびイラストの背景を透過できる。
- WebP、GIF、MP4アニメーションを作成できる。
- フルカラーSVG、輪郭線SVG、シルエットSVGを作成できる。
- 外形のみ、および内部線付きDXFを作成できる。
- 操作イベントとエラーがログへ残る。
